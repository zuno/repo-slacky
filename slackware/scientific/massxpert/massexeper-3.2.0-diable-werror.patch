diff -aur massxpert-3.2.0/CMakeLists.txt massxpert-3.2.0.new/CMakeLists.txt
--- massxpert-3.2.0/CMakeLists.txt	2011-10-22 21:50:35.000000000 +0200
+++ massxpert-3.2.0.new/CMakeLists.txt	2012-11-15 20:53:11.277229024 +0100
@@ -23,21 +23,21 @@
 
 #############################################################
 # Enable warnings and treat them as errors, on GNU/Linux only
-IF (UNIX)
-  SET (PEDANTIC TRUE CACHE BOOL "Should we compile with -Wall -Werror.")
-  
-  IF (PEDANTIC)
-    ADD_DEFINITIONS (-Wall -Werror)
-  ENDIF (PEDANTIC)
-ENDIF (UNIX)
-
-IF (WIN32)
-  SET (PEDANTIC TRUE CACHE BOOL "Should we compile with -Wall.")
-  
-  IF (PEDANTIC)
-    ADD_DEFINITIONS (-Wall)
-  ENDIF (PEDANTIC)
-ENDIF (WIN32)
+#IF (UNIX)
+#  SET (PEDANTIC TRUE CACHE BOOL "Should we compile with -Wall -Werror.")
+#  
+#  IF (PEDANTIC)
+#    ADD_DEFINITIONS (-Wall -Werror)
+#  ENDIF (PEDANTIC)
+#ENDIF (UNIX)
+#
+#IF (WIN32)
+#  SET (PEDANTIC TRUE CACHE BOOL "Should we compile with -Wall.")
+#  
+#  IF (PEDANTIC)
+#    ADD_DEFINITIONS (-Wall)
+#  ENDIF (PEDANTIC)
+#ENDIF (WIN32)
 
 #############################################################
 #############################################################
Solo in massxpert-3.2.0.new/: CMakeLists.txt~
